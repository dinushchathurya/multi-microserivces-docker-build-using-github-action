name: Build and Push Docker Images
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Get changed services
        id: get-changed-services
        run: |
<<<<<<< HEAD
          CHANGED_SERVICES=$(git diff --name-only ${{ github.event.before }}...${{ github.sha }} | grep "/" | cut -d'/' -f1 | sort -u)
          echo "::set-output name=services::$CHANGED_SERVICES"

      
=======
            git diff --name-only origin/master | cut -d/ -f1 | sort -u > modified_microservices.txt
            cat modified_microservices.txt
            echo "Modified microservices: $(cat modified_microservices.txt)"
            echo "::set-output name=microservices::$(cat modified_microservices.txt)"
>>>>>>> b61b160866aedd71bd11f67346b17a560855e53a
